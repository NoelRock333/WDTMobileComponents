import React from 'react';
import { View } from 'react-native';
import { Text, Button } from 'native-base';

const Tab = () => (
  <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
    <Text>Here is a tab</Text>
  </View>
);

export default Tab;
